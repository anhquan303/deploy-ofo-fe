/*
 * CustomTable Messages
 *
 * This contains all the text for the CustomTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomTable component!',
  },
});
