/*
 * CustomTableResponsive Messages
 *
 * This contains all the text for the CustomTableResponsive component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomTableResponsive';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomTableResponsive component!',
  },
});
