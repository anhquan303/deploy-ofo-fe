// import { selectSellerTurnoverDomain } from '../selectors';

describe('selectSellerTurnoverDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
