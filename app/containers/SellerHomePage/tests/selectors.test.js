// import { selectSellerHomePageDomain } from '../selectors';

describe('selectSellerHomePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
