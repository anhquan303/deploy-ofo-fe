// import { selectSellerActionProductDomain } from '../selectors';

describe('selectSellerActionProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
