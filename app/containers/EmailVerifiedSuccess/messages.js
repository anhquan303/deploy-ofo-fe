/*
 * EmailVerifiedSuccess Messages
 *
 * This contains all the text for the EmailVerifiedSuccess container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EmailVerifiedSuccess';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmailVerifiedSuccess container!',
  },
});
