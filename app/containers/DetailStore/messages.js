/*
 * DetailStore Messages
 *
 * This contains all the text for the DetailStore container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DetailStore';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DetailStore container!',
  },
});
