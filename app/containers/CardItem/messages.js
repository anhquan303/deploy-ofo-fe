/*
 * CardItem Messages
 *
 * This contains all the text for the CardItem container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CardItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardItem container!',
  },
});
