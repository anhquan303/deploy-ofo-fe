/*
 * DetailRegister Messages
 *
 * This contains all the text for the DetailRegister container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DetailRegister';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DetailRegister container!',
  },
});
