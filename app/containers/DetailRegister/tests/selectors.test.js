// import { selectDetailRegisterDomain } from '../selectors';

describe('selectDetailRegisterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
