// import { selectUserResetPasswordDomain } from '../selectors';

describe('selectUserResetPasswordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
