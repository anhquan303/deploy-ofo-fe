/*
 * UserResetPassword Messages
 *
 * This contains all the text for the UserResetPassword container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserResetPassword';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserResetPassword container!',
  },
});
