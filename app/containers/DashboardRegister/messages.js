/*
 * DashboardRegister Messages
 *
 * This contains all the text for the DashboardRegister container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardRegister';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardRegister container!',
  },
});
