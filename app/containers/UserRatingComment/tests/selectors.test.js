// import { selectUserRatingCommentDomain } from '../selectors';

describe('selectUserRatingCommentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
