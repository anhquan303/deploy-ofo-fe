/*
 * SellerRegister Messages
 *
 * This contains all the text for the SellerRegister container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SellerRegister';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SellerRegister container!',
  },
});
