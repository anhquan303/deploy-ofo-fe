/*
 * UserSetting Messages
 *
 * This contains all the text for the UserSetting container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserSetting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserSetting container!',
  },
});
