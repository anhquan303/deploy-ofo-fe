// import { selectSellerManagerProductDomain } from '../selectors';

describe('selectSellerManagerProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
