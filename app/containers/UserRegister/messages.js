/*
 * UserRegister Messages
 *
 * This contains all the text for the UserRegister container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserRegister';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserRegister container!',
  },
});
