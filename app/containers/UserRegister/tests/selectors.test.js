// import { selectUserRegisterDomain } from '../selectors';

describe('selectUserRegisterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
