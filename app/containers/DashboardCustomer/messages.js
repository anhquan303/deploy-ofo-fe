/*
 * DashboardCustomer Messages
 *
 * This contains all the text for the DashboardCustomer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardCustomer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardCustomer container!',
  },
});
