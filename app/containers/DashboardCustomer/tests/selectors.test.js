// import { selectDashboardCustomerDomain } from '../selectors';

describe('selectDashboardCustomerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
