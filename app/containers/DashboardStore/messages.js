/*
 * DashboardStore Messages
 *
 * This contains all the text for the DashboardStore container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DashboardStore';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DashboardStore container!',
  },
});
