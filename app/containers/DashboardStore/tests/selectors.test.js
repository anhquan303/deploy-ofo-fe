// import { selectDashboardStoreDomain } from '../selectors';

describe('selectDashboardStoreDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
