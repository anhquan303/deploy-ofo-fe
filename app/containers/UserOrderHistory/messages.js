/*
 * UserOrderHistory Messages
 *
 * This contains all the text for the UserOrderHistory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserOrderHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserOrderHistory container!',
  },
});
