// import { selectDetailCustomerDomain } from '../selectors';

describe('selectDetailCustomerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
