// import { selectUserDetailOrderDomain } from '../selectors';

describe('selectUserDetailOrderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
