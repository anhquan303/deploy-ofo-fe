/*
 * UserDetailOrder Messages
 *
 * This contains all the text for the UserDetailOrder container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserDetailOrder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserDetailOrder container!',
  },
});
