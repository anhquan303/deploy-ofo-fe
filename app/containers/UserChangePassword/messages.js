/*
 * UserChangePassword Messages
 *
 * This contains all the text for the UserChangePassword container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserChangePassword';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserChangePassword container!',
  },
});
