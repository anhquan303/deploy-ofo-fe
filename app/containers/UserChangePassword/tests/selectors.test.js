// import { selectUserChangePasswordDomain } from '../selectors';

describe('selectUserChangePasswordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
