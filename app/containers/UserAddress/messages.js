/*
 * UserAddress Messages
 *
 * This contains all the text for the UserAddress container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserAddress';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserAddress container!',
  },
});
