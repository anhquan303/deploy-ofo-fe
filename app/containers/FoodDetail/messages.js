/*
 * FoodDetail Messages
 *
 * This contains all the text for the FoodDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FoodDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FoodDetail container!',
  },
});
