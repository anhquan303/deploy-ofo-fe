// import { selectSellerOrderDetailDomain } from '../selectors';

describe('selectSellerOrderDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
