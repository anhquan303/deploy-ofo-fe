/*
 * SellerOrderDetail Messages
 *
 * This contains all the text for the SellerOrderDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SellerOrderDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SellerOrderDetail container!',
  },
});
